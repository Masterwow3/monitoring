name: monitoring
on:
  schedule:
    - cron: "*/15 * * * *"
#  push:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Test ipv4 website status code
        run: sh websiteV4.sh
      - name: Test ipv6 website status code
        run: sh websiteV6.sh
      - name: Validate certificate
        run: sh certificate.sh
